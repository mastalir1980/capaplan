{
  "name": "capacity-planning-tool",
  "version": "1.0.0",
  "description": "A simple capacity planning tool for managing employees, teams, projects, and assignments",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && node src/server.js",
    "start:frontend": "cd frontend && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "capacity planning",
    "resource management",
    "project management"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
